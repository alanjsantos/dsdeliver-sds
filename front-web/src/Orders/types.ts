export type Product =  {
        id: number
        name: string
        description: string
        price: number
        imageUri: string
}

export type OderLocationData = {
        latitude: number;
        longitude: number;
        address: string;
}